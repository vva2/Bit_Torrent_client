// define peer class here

class Peer {
    constructor({ip, port}) {
        this.ip = ip;
        this.port = port;
    }

    download(requested, downloaded) {
        
    }
}

module.exports = {
    Peer: Peer
};